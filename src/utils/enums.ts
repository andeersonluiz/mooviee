export enum MovieListType {
  POPULAR,
  TOP_RATED,
  UPCOMING,
}

export enum SerieListType {
  POPULAR,
  TOP_RATED,
  ON_THE_AIR,
}

export enum TrendingType {
  DAY = 'day',
  WEEK = 'week',
}

export enum ItemType {
  MOVIE,
  SERIE,
}
